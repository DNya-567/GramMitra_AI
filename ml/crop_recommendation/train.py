"""
Train the crop recommendation model.
Expected dataset columns: soil_type, N, P, K, rainfall, region, label(crop)
Output: model.pkl in this same folder, loaded by
backend/app/services/crop_service.py
"""
# TODO: load dataset/, train (e.g. RandomForestClassifier), save model.pkl
